@model IEnumerable<StudyDoIT.Models.NLP.KeyWord>

@{
    ViewBag.Title = "Ma trận xác suất từ";
    Layout = "~/Areas/AdminIT/Views/Shared/_LayoutAdmin.cshtml";
}

<div>
    <div class="row">
        <div class="col-lg-12">
            <section class="panel">
                <header class="panel-heading">
                    <div class="panel-actions">
                        <a href="#" class="panel-action panel-action-toggle" data-panel-toggle></a>
                    </div>
                    <h2 class="panel-title">Load ma trận xác suất từ</h2>
                </header>
                <div class="panel-body">
                    @using (Html.BeginForm("MatrixProbabilityWords", "TranningResuft", FormMethod.Post, new { @class = "form-horizontal form-bordered", enctype = "multipart/form-data" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true)
                        <div class="form-group">
                            <label class="col-md-3 control-label" for="inputPlaceholder">Chọn sản phẩm</label>
                            <div class="col-md-6">
                                @Html.DropDownList("GroupComentId", (SelectList)ViewBag.GroupComent, "Chọn sản phẩm", new { @class = "form-control", id = "GroupComentId" })
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-md-3 control-label" for="inputPlaceholder">Chọn nhóm từ khóa</label>
                            <div class="col-md-6">
                                @Html.DropDownList("GroupWordId", (SelectList)ViewBag.GroupWord, "Chọn nhóm từ khóa", new { @class = "form-control", id = "GroupWordId" })
                            </div>
                        </div>
                        <div class="form-group text-center">
                            <div class="col-md-4">
                                <button type="submit" class="mb-xs mt-xs mr-xs btn btn-success"><i class="fa fa-search-plus"></i> Load chỉ số</button>
                            </div>
                        </div>
                    }
                </div>
            </section>
        </div>
    </div>

    @if (Model != null)
    {
        <section class="panel">
            <header class="panel-heading">
                <div class="panel-actions">
                    <a href="#" class="panel-action panel-action-toggle" data-panel-toggle></a>
                </div>
                <h2 class="panel-title">Ma trận xác suất từ</h2>
            </header>
            <div class="panel-body">
                <table class="table table-bordered table-striped mb-none" id="datatable-tabletools"
                       data-swf-path="~/Areas/AdminIT/Content/vendor/jquery-datatables/extras/TableTools/swf/copy_csv_xls_pdf.swf">
                    <thead>
                        <tr>
                            <th>Từ khóa</th>
                            <th>xs 1 điểm</th>
                            <th>xs 2 điểm</th>
                            <th>xs 3 điểm</th>
                            <th>xs 4 điểm</th>
                            <th>xs 5 điểm</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr class="gradeX">
                                <td>@Html.DisplayFor(modelItem => item.Word)</td>
                                <td>@Html.DisplayFor(modelItem => item.P1)</td>
                                <td>@Html.DisplayFor(modelItem => item.P2)</td>
                                <td>@Html.DisplayFor(modelItem => item.P3)</td>
                                <td>@Html.DisplayFor(modelItem => item.P4)</td>
                                <td>@Html.DisplayFor(modelItem => item.P5)</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </section>
    }
</div>

